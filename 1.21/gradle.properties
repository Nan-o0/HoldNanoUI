org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# 1.21.1, whose mappings are already in the loom cache from an earlier build here.
# Retargeting is a three line change if Star turns out to be on a different 1.21.
minecraft_version=1.21.1
yarn_mappings=1.21.1+build.3
loader_version=0.16.10
loom_version=1.11-SNAPSHOT
fabric_api_version=0.115.0+1.21.1

mod_version=1.1.2
maven_group=gg.nano
archives_base_name=nanoui
